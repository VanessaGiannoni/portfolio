import { Noto_Sans, Inter } from 'next/font/google';

export const notoSans = Noto_Sans({
  subsets: ['latin'],
  weight: ['100', '400', '700'],
});

export const inter = Inter({
  subsets: ['latin'],
  weight: ['100', '400', '700'],
});
